# SupiChat environment (Docker/local)

# Next.js basePath and Socket.IO path
NEXT_PUBLIC_BASE_PATH=/MyChatApp
NEXT_PUBLIC_SIGNALING_PATH=/MyChatApp/socket.io

# Optional: STUN/TURN for WebRTC
NEXT_PUBLIC_STUN_1=stun:stun.l.google.com:19302
# NEXT_PUBLIC_TURN_URL=
# NEXT_PUBLIC_TURN_USERNAME=
# NEXT_PUBLIC_TURN_CRED=

# TURN shared secret for coturn (only needed if enabling the 'turn' profile)
# Generate a random string for local testing if you enable coturn
TURN_SECRET=dev-secret-change-me

# OpenAI configuration for translation API
# Set your key here for Docker runs (local or prod) and it will be available to the web app
OPENAI_API_KEY=
# Choose a smaller/cheaper model for translations by default; you can switch to nano/mini as needed
OPENAI_TRANSLATE_MODEL=gpt-5-mini
